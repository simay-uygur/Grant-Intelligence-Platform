class WebSearchClient:
    def search(self, query: str) -> list[dict[str, str]]:
        # Placeholder until a real search provider is chosen.
        return [{"title": "Sample search result", "query": query}]
