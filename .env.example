# Copy to .env.local and adjust as needed. Nothing here is secret — this app
# has no API keys, no auth tokens, and no database credentials.

# "mock" (default) uses the local demo catalogue. "api" checks backend status
# and sends general chat and grant searches to FastAPI. Selected-grant Q&A and
# application drafting remain local.
VITE_API_MODE=mock

# Backend origin. The frontend adapter appends the versioned /api/v1 paths.
VITE_API_URL=http://127.0.0.1:8000
APP_NAME=Grant Intelligence Backend
APP_VERSION=0.1.0
API_PREFIX=/api/v1
DEBUG=true
